# Dependencies
/node_modules
/.pnp
.pnp.js

# Testing
/coverage

# Playwright (e2e test output and cache — do not push)
/test-results/
/playwright-report/
/blob-report/
/.playwright/

# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env
.env*
.env*.local

# Database
*.sql

# Scratch files
scratch/

# Local backup copies of pages/components (kept on disk, never committed)
.backup/
*.mykoott-backup.js
*.before-*.jsx

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Keep the template committed (it holds no secrets)
!.env.example

# Claude Code personal settings (launch.json stays committed — it's shared config)
.claude/settings.local.json
